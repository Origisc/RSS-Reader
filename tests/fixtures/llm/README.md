# Stage 3 offline fixtures

`stage3_article.json` is deterministic input for the Stage 3 AI acceptance
test. It contains only public test text and expected Mock Provider output.

Never add API keys, private article content, production Base URLs, or real
Provider responses to this directory.

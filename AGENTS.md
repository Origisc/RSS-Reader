# AGENTS

本文件是 Mercury 项目的开发规则。任何 Coding Agent 或开发者在修改代码、配置、文档、测试、构建脚本前，都必须遵守本文件。

目标：少写无关功能，优先保证本地优先、隐私、跨平台、核心 RSS 阅读体验和可维护架构。

---

## 1. 最高优先级规则

以下规则高于所有具体功能需求。

### 1.1 本地优先

必须：

- 应用无需注册、登录、订阅即可使用。
- 订阅源、文章缓存、阅读记录、笔记、标签、配置默认保存在本地。
- 不主动采集、上传、同步用户数据。
- 所有网络请求必须有明确用途：拉取订阅源、抓取文章、用户主动配置的 LLM 调用。

禁止：

- 禁止把云同步、账号系统作为核心功能前置条件。
- 禁止未经用户配置把文章内容发送给第三方服务。

### 1.2 跨平台

必须：

- 支持 Windows、Linux、macOS。
- 文件读写显式使用 UTF-8。

### 1.3 LLM 中立

必须：

- 所有 AI 能力都通过统一 Provider 抽象调用。
- 不把任何单一厂商、模型名、Base URL、API Key 写死到业务逻辑。
- Summary Agent、Translation Agent、Tag Agent 必须可配置 Prompt。
- AI 功能不能成为基础阅读功能的强依赖。

---

## 2. 技术栈规则

### 2.1 Python

- 使用 Python 3.13。
- 新代码必须兼容 Python 3.13。
- 不引入阻碍 Python 3.13 的依赖。

### 2.2 依赖管理

- 使用 `uv`。
- 安装、运行、测试文档优先写 `uv` 命令。
- 不把 Poetry、Pipenv、Conda 作为主包管理方案。
- 新依赖必须有明确用途，避免重型依赖。

### 2.3 UI

- 主 UI 框架使用 PySide6。
- Flet 只作为快速原型备选，不作为正式主实现。
- UI 层只负责展示和交互，不直接承担抓取、数据库、LLM 调用等业务逻辑。

---

## 3. Feed、文章与清洗规则

必须：

- 支持常见 RSS / Atom Feed。
- OPML 导入要能处理分组、重复源、无效源。
- 刷新订阅时要去重，避免重复文章。
- 缓存文章时保留原始内容与清洗后内容。
- Cleaned HTML / Markdown 应保留标题层级、链接、图片、列表、表格、代码块。
- 清洗失败时不能导致文章不可读，必须有 fallback。

禁止：

- 禁止只按标题去重。
- 禁止清洗时无理由删除表格、图片、链接等关键信息。
- 禁止因 AI 功能失败影响基础阅读。

---

## 4. Agent 规则

### 4.1 Summary Agent

必须支持：

- 摘要语言设置。
- 详细程度设置。
- 自定义 Prompt。
- 通过 LLM Provider 抽象调用。
- 失败时返回可理解错误，不影响文章阅读。

### 4.2 Translation Agent

必须支持：

- 自定义 Prompt。
- 原文与译文段落对照。
- 长文章分段处理。
- 保留段落顺序。
- 失败时保留原文可读。

### 4.3 Tag Agent

- Tag Agent 是选做功能。
- 不得为了 Tag Agent 提前复杂化核心架构。
- 若实现，必须复用统一 LLM Provider 和 Prompt 配置机制。

---

## 5. 本地存储规则

必须：

- 用户数据默认本地保存。
- 删除订阅源、文章缓存、笔记、标签前必须避免误删。

建议：

- 将订阅源、文章、清洗内容、AI 结果、阅读状态、配置分表或分模块管理。
- 对大文本内容考虑缓存策略，避免 UI 卡顿。

---

## 6. 国际化规则

必须：

- UI 支持英文和简体中文。
- 语言切换无需重启。
- 错误提示、按钮、菜单、设置项都要可翻译。

---

## 7. 测试要求

涉及以下内容的改动必须增加或更新测试：

- Feed / OPML 解析。
- 文章去重。
- Reader 清洗。
- HTML / Markdown 转换。
- LLM Provider 抽象。
- Summary / Translation 工作流。
- 本地存储和迁移。

测试不能依赖真实在线 LLM、真实 API Key 或不稳定网络。

---

## 8. Git 与文档留痕

必须：

- 提交信息清楚说明改动目的。
- 复杂任务保留过程记录、决策理由和后续待办。

禁止：

- 禁止大而混乱的提交。
- 禁止把无关格式化、重构和功能改动混在一起。

---

## 9. 每次改动前检查

提交或交付前必须确认：

- 是否仍然符合本地优先？
- 是否泄露或上传了用户数据？
- 是否破坏 Windows、Linux、macOS 兼容性？
- 是否把 LLM 厂商写死了？
- 是否有必要测试？
- 是否更新了相关文档？
- 是否留下了无用依赖或临时代码？